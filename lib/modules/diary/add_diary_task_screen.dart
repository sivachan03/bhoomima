import 'package:flutter/material.dart';

class AddDiaryTaskScreen extends StatelessWidget {
  const AddDiaryTaskScreen({super.key});
  @override
  Widget build(BuildContext c) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add diary task')),
      body: const Center(child: Text('TODO: implement diary form')),
    );
  }
}
