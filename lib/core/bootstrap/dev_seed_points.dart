import 'package:isar/isar.dart';

/// No-op dev seeder stub to satisfy stale build references safely.
/// This function intentionally does nothing.
Future<void> seedDemoPartitionIfEmpty(
  Isar isar, {
  required int propertyId,
}) async {
  // Intentionally left blank. Used only to prevent build failures
  // if an old import/reference still exists in incremental caches.
}
